"""GAF 工具的纯逻辑内核。

这里的代码不认识 Telegram，也不认识 HTTP：
输入是本地文件路径和参数，输出是结果数据和生成的文件。

两个前端各自套外壳：
- 机器人：tam/gaf/*.py      下载附件 -> 调内核 -> send_document
- 网页：  tam/api.py         上传接口 -> 调内核 -> 任务进度 -> 下载结果

这样两边跑的是同一套逻辑，而不是两份会逐渐跑偏的复制品，
并且内核可以直接单测，不需要伪造 update / context。
"""
